import 'package:flutter/material.dart';

class TrainingBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Container(
        child: new Icon(
          Icons.sports_rounded,
          size: 150,
          color: Colors.blue,
        ),
      ),
    );
  }
}
